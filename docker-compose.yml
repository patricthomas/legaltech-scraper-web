services:
  legaltech-scraper:
    build: .
    container_name: legaltech-scraper
    ports:
      - "5051:5050"
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
      - DOCKER_ENV=1
    volumes:
      - scraper_data:/app/data

volumes:
  scraper_data:
